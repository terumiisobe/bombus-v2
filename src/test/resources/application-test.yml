spring:
  datasource:
    url: jdbc:h2:mem:bombus-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: false

twilio:
  auth-token: test-auth-token
  public-base-url: https://example.test
