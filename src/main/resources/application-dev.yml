# Dev profile: uses the base Postgres datasource (local docker container),
# adds developer conveniences. Run with: --spring.profiles.active=dev
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
